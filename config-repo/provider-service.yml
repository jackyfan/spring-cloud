server:
  port: 20000

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery:8061/eureka/
      
logging:
  pattern: 
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"
